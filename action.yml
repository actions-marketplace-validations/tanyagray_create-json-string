name: 'create-json-string'
description: 'Create a JSON string from a set of provided inputs'
outputs:
  json_string:
    description: 'A JSON string of the provided values'
runs:
  using: 'node12'
  main: 'index.js'
branding:
  icon: 'book-open'  
  color: 'yellow'
